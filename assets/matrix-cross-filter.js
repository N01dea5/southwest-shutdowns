/* matrix-cross-filter.js — legacy, now inert.
 *
 * This script targeted <select> filter elements that no longer exist.
 * app.js uses <button> elements for the tri-state column filter cycle.
 * The old MutationObserver and setInterval have been removed to eliminate
 * cascading DOM-mutation loops.
 */
